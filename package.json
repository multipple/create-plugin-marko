{
  "name": "test-plugin",
  "description": "Test marko plugin setup",
  "version": "1.0.0",
  "private": true,
  "scripts": {
		"start": "cd ./.sandbox && yarn start",
		"test": "cd ./.sandbox && yarn test:dev"
	},
  "main": "src/index.marko",
  "workspaces": [
    ".sandbox"
  ],
  "dependencies": {
    "marko": "4.24.0"
  }
}